@model IEnumerable<SayGood2.Models.Detail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UpDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Desc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ToWho)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UpDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ToWho)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UpdateId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UpdateId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UpdateId })
        </td>
    </tr>
}

</table>
